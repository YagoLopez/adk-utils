interface:
  display_name: "Adk Utils"
  short_description: "Repo-specific patterns and workflows for adk-utils"
  default_prompt: "Use the adk-utils repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true